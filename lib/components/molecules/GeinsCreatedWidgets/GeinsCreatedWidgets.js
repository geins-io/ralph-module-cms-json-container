import { shallowMount } from '@vue/test-utils';
import GeinsCreatedWidgets from './GeinsCreatedWidgets.vue';

describe('GeinsCreatedWidgets.vue', () => {
  it('renders a component', () => {
    const component = shallowMount(GeinsCreatedWidgets);
  });
});
