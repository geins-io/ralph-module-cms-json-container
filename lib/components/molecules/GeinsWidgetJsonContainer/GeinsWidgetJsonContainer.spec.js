import { shallowMount } from '@vue/test-utils';
import GeinsWidgetJsonContainer from './GeinsWidgetJsonContainer.vue';

describe('GeinsWidgetJsonContainer.vue', () => {
  it('renders a component', () => {
    const component = shallowMount(GeinsWidgetJsonContainer);
  });
});
